# {{ issue.key }} comments

{% if comments.size > 0 %}{% for comment in comments %}## {{ comment.date }} — {{ comment.author }}

Comment {{ comment.id }} · {{ comment.created }}{{ comment.updatedNote }}

{{ comment.body }}{% unless forloop.last %}

---

{% endunless %}{% endfor %}{% else %}_No visible comments._{% endif %}
